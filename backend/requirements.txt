fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6

# Solver (step 2): Tourist Trip Design Problem via OR-Tools routing
ortools>=9.10

# MCP server (step 5 phase 2): expose the planner as tools for external AI clients
mcp>=1.2

# Database (scale-up phase 1): durable storage for cities/POIs/plans via Postgres
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
